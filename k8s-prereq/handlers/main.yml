# handlers file for k8s-prereq
---

# FIX: Replaced deprecated 'with_items' with modern 'loop' syntax.
- name: start service
  service:
    name: "{{ item }}"
    state: started
    enabled: yes
  loop: "{{ services }}"
  become: true
